---
const projects = [
  {
    title: "TIENDA SUSHI 🍣",
    category: "E-Commerce Real",
    desc: "E-commerce desarrollado para restaurante real, con carrito dinámico, gestión de favoritos y envío de pedido directo a WhatsApp.",
    img: "tienda-sushi.webp",
    tags: ["React", "Tailwind CSS", "Vercel", "Vite"],
    repo: "https://github.com/ArielCamposs/tienda-sushi",
    link: "https://tienda-sushi.vercel.app/",
    featured: true,
    features: [
      "Carrito de compras funcional",
      "Control visual de stock",
      "Favoritos en localStorage",
      "Integración WhatsApp API",
    ],
  },
  {
    title: "TIENDA E-COMMERCE PRO 🛒",
    category: "Full Stack",
    desc: "Plataforma profesional de comercio electrónico con carrito dinámico y cierre de venta directo con API de WhatsApp.",
    img: "tienda-ecommerce.webp",
    tags: ["React", "Tailwind CSS", "Vercel", "Vite"],
    link: "https://tienda-ecommerce-ten.vercel.app/",
    repo: "https://github.com/ArielCamposs/tienda-ecommerce",
    features: [
      "Carrito persistente",
      "Dashboard visual",
      "Mobile First",
    ],
    result: "+35 pedidos semanales 📈"
  },
  {
    title: "PELUQUERÍA DELTA ✂️",
    category: "Landing Page",
    desc: "Landing page creada para captar clientes mediante SEO Local y CTA directo a WhatsApp.",
    img: "landing-page.webp",
    tags: ["Astro", "Tailwind CSS", "Responsive"],
    link: "https://landing-page-peluqueria-delta.vercel.app/",
    repo: "https://github.com/ArielCamposs/landing-page-informativa",
    features: [
      "SEO Local optimizado",
      "WhatsApp directo",
      "Carga ultra rápida",
    ],
    result: "Mejora notable de captación ✨"
  },
  {
    title: "CATÁLOGO DIGITAL 📦",
    category: "Showcase Web",
    desc: "Exhibición digital del catálogo con filtros dinámicos y contacto directo con vendedor.",
    img: "catalogo-web.webp",
    tags: ["React", "Vite", "CSS Modules"],
    link: "https://pagina-web-catalogo.vercel.app/",
    repo: "https://github.com/ArielCamposs/Pagina-Web-Catalogo-Productos",
    features: [
      "Filtros por categoría",
      "Detalle visual",
      "UI ligera y rápida",
    ],
    result: "Digitalización de inventario 🔄"
  },
];
---

<section id="proyectos" class="projects-section">
  <!-- Background Effects -->
  <div class="bg-orb bg-orb-1"></div>
  <div class="bg-orb bg-orb-2"></div>
  <div class="bg-orb bg-orb-3"></div>
  
  <!-- Decorative Lines -->
  <div class="deco-line deco-line-1"></div>
  <div class="deco-line deco-line-2"></div>

  <div class="container">
    <!-- Header -->
    <header class="section-header">
      <span class="code-tag">// SELECTED_WORKS</span>
      <h2 class="section-title">
        MIS <br/>
        <span class="gradient-text">PROYECTOS</span>
      </h2>
      <p class="section-subtitle">Proyectos reales que demuestran mis habilidades</p>
    </header>

    <!-- Bento Grid -->
    <div class="bento-grid">
      {projects.map((project, index) => (
        <article 
          class={`project-card ${project.featured ? 'featured' : ''}`}
          style={`--delay: ${index * 0.1}s`}
        >
          <!-- Glow Border Effect -->
          <div class="card-glow"></div>
          
          <!-- Card Content -->
          <div class="card-inner">
            <!-- Image Section -->
            <div class="card-image">
              <img src={project.img} alt={project.title} loading="lazy" />
              <div class="image-overlay"></div>
              
              <!-- Category Badge -->
              <span class="category-badge">{project.category}</span>
              
              <!-- Quick Actions (visible on hover) -->
              <div class="quick-actions">
                <a href={project.link} target="_blank" class="action-btn primary" title="Ver Demo">
                  <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path><polyline points="15 3 21 3 21 9"></polyline><line x1="10" y1="14" x2="21" y2="3"></line></svg>
                </a>
                {project.repo && (
                  <a href={project.repo} target="_blank" class="action-btn secondary" title="Ver Código">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path></svg>
                  </a>
                )}
              </div>
            </div>

            <!-- Content Section -->
            <div class="card-content">
              <h3 class="project-title">{project.title}</h3>
              <p class="project-desc">{project.desc}</p>
              
              <!-- Features List -->
              {project.features && (
                <ul class="features-list">
                  {project.features.map((feat) => (
                    <li>
                      <span class="feature-icon">✦</span>
                      {feat}
                    </li>
                  ))}
                </ul>
              )}

              <!-- Result Badge -->
              {project.result && (
                <div class="result-badge">{project.result}</div>
              )}

              <!-- Tags -->
              <div class="tags-container">
                {project.tags.map((tag) => (
                  <span class="tech-tag">#{tag}</span>
                ))}
              </div>

              <!-- CTA Buttons -->
              <div class="cta-buttons">
                <a href={project.link} target="_blank" class="btn-demo">
                  <span>Ver Demo</span>
                  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                </a>
                {project.repo && (
                  <a href={project.repo} target="_blank" class="btn-code">
                    Código
                  </a>
                )}
              </div>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<!-- Infinite Scroll Marquee -->
<div class="marquee-container">
  <div class="marquee">
    <div class="track">
      <p>👨‍💻 Desarrollador Web Full Stack Junior Disponible 🚀</p>
      <p>💼 Contrataciones & Proyectos Freelance 📩</p>
      <p>⚡ React • Astro • Tailwind • APIs • UX/UI 🎨</p>
      <p>📍 Chile | Remoto LATAM 🌍</p>
      <p>👨‍💻 Desarrollador Web Full Stack Junior Disponible 🚀</p>
      <p>💼 Contrataciones & Proyectos Freelance 📩</p>
      <p>⚡ React • Astro • Tailwind • APIs • UX/UI 🎨</p>
      <p>📍 Chile | Remoto LATAM 🌍</p>
    </div>
  </div>
</div>

<style>
  /* ===== SECTION BASE ===== */
  .projects-section {
    position: relative;
    padding: 8rem 0;
    background: #0a0a0a;
    overflow: hidden;
    min-height: 100vh;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 1.5rem;
    position: relative;
    z-index: 10;
  }

  /* ===== BACKGROUND EFFECTS ===== */
  .bg-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(120px);
    pointer-events: none;
    opacity: 0.5;
  }

  .bg-orb-1 {
    top: -100px;
    right: -100px;
    width: 500px;
    height: 500px;
    background: linear-gradient(135deg, #00ffff 0%, #bc13fe 100%);
    animation: float-orb 8s ease-in-out infinite;
  }

  .bg-orb-2 {
    bottom: 20%;
    left: -150px;
    width: 400px;
    height: 400px;
    background: linear-gradient(135deg, #ff0099 0%, #ccff00 100%);
    animation: float-orb 10s ease-in-out infinite reverse;
  }

  .bg-orb-3 {
    top: 50%;
    right: 10%;
    width: 300px;
    height: 300px;
    background: #ccff00;
    opacity: 0.2;
    animation: float-orb 6s ease-in-out infinite;
  }

  @keyframes float-orb {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(30px, -30px) scale(1.1); }
  }

  /* Decorative Lines */
  .deco-line {
    position: absolute;
    background: linear-gradient(90deg, transparent, #ccff00, transparent);
    height: 1px;
    pointer-events: none;
  }

  .deco-line-1 {
    top: 15%;
    left: 0;
    width: 100%;
    opacity: 0.3;
  }

  .deco-line-2 {
    bottom: 25%;
    left: 0;
    width: 100%;
    opacity: 0.2;
  }

  /* ===== HEADER ===== */
  .section-header {
    margin-bottom: 4rem;
    text-align: center;
  }

  .code-tag {
    display: inline-block;
    font-family: monospace;
    font-size: 1rem;
    color: #ccff00;
    letter-spacing: 0.2em;
    margin-bottom: 1rem;
    text-shadow: 0 0 20px rgba(204, 255, 0, 0.5);
  }

  .section-title {
    font-size: clamp(3rem, 10vw, 6rem);
    font-weight: 900;
    color: white;
    text-transform: uppercase;
    line-height: 0.9;
    letter-spacing: -0.02em;
  }

  .gradient-text {
    background: linear-gradient(135deg, #ff0099 0%, #bc13fe 50%, #00ffff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-subtitle {
    margin-top: 1.5rem;
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 300;
  }

  /* ===== BENTO GRID ===== */
  .bento-grid {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .project-card.featured {
      grid-column: span 2;
    }
  }

  @media (min-width: 1024px) {
    .bento-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
    
    .project-card.featured {
      grid-column: span 2;
      grid-row: span 1;
    }
    
    .project-card.featured .card-inner {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
    }
    
    .project-card.featured .card-image {
      height: 100%;
      min-height: 400px;
    }
    
    .project-card.featured .card-content {
      padding: 2rem;
    }
  }

  /* ===== PROJECT CARD ===== */
  .project-card {
    position: relative;
    border-radius: 24px;
    opacity: 0;
    animation: fadeInUp 0.6s ease forwards;
    animation-delay: var(--delay);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Glow Effect */
  .card-glow {
    position: absolute;
    inset: -2px;
    border-radius: 26px;
    background: linear-gradient(135deg, #ccff00 0%, #00ffff 50%, #ff0099 100%);
    opacity: 0;
    transition: opacity 0.4s ease;
    z-index: 0;
  }

  .project-card:hover .card-glow {
    opacity: 1;
  }

  .card-inner {
    position: relative;
    background: linear-gradient(145deg, rgba(20, 20, 20, 0.9) 0%, rgba(10, 10, 10, 0.95) 100%);
    backdrop-filter: blur(20px);
    border-radius: 24px;
    overflow: hidden;
    height: 100%;
    display: flex;
    flex-direction: column;
    z-index: 1;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .project-card:hover .card-inner {
    border-color: rgba(204, 255, 0, 0.3);
    transform: translateY(-8px);
    box-shadow: 
      0 25px 50px -12px rgba(0, 0, 0, 0.5),
      0 0 60px -15px rgba(204, 255, 0, 0.3);
  }

  /* ===== CARD IMAGE ===== */
  .card-image {
    position: relative;
    height: 220px;
    overflow: hidden;
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .project-card:hover .card-image img {
    transform: scale(1.1);
  }

  .image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      transparent 0%,
      rgba(10, 10, 10, 0.3) 50%,
      rgba(10, 10, 10, 0.9) 100%
    );
    transition: opacity 0.4s ease;
  }

  .category-badge {
    position: absolute;
    top: 1rem;
    left: 1rem;
    padding: 0.5rem 1rem;
    background: linear-gradient(135deg, #ff0099 0%, #bc13fe 100%);
    color: white;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 50px;
    box-shadow: 0 4px 15px rgba(255, 0, 153, 0.4);
    z-index: 10;
  }

  /* Quick Actions */
  .quick-actions {
    position: absolute;
    top: 1rem;
    right: 1rem;
    display: flex;
    gap: 0.5rem;
    opacity: 0;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    z-index: 10;
  }

  .project-card:hover .quick-actions {
    opacity: 1;
    transform: translateY(0);
  }

  .action-btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }

  .action-btn.primary {
    background: #ccff00;
    color: #0a0a0a;
  }

  .action-btn.secondary {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .action-btn:hover {
    transform: scale(1.15);
  }

  .action-btn.primary:hover {
    box-shadow: 0 0 20px rgba(204, 255, 0, 0.6);
  }

  .action-btn.secondary:hover {
    background: white;
    color: #0a0a0a;
  }

  /* ===== CARD CONTENT ===== */
  .card-content {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }

  .project-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: white;
    margin-bottom: 0.75rem;
    line-height: 1.2;
    transition: color 0.3s ease;
  }

  .project-card:hover .project-title {
    color: #00ffff;
  }

  .project-desc {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  /* Features List */
  .features-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .features-list li {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.6);
    background: rgba(255, 255, 255, 0.05);
    padding: 0.35rem 0.75rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .feature-icon {
    color: #ccff00;
    font-size: 0.65rem;
  }

  /* Result Badge */
  .result-badge {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1rem;
    background: linear-gradient(135deg, rgba(204, 255, 0, 0.15) 0%, rgba(0, 255, 255, 0.1) 100%);
    border: 1px solid rgba(204, 255, 0, 0.3);
    border-radius: 8px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #ccff00;
    margin-bottom: 1rem;
  }

  /* Tags */
  .tags-container {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1.25rem;
  }

  .tech-tag {
    font-size: 0.7rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.5);
    padding: 0.35rem 0.75rem;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 6px;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .tech-tag:hover {
    border-color: #ccff00;
    color: #ccff00;
    background: rgba(204, 255, 0, 0.1);
  }

  /* CTA Buttons */
  .cta-buttons {
    display: flex;
    gap: 0.75rem;
    margin-top: auto;
  }

  .btn-demo {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, #ccff00 0%, #a8cc00 100%);
    color: #0a0a0a;
    font-size: 0.85rem;
    font-weight: 700;
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(204, 255, 0, 0.3);
  }

  .btn-demo:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(204, 255, 0, 0.5);
  }

  .btn-demo svg {
    transition: transform 0.3s ease;
  }

  .btn-demo:hover svg {
    transform: translateX(4px);
  }

  .btn-code {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    background: transparent;
    color: white;
    font-size: 0.85rem;
    font-weight: 600;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-code:hover {
    border-color: white;
    background: rgba(255, 255, 255, 0.1);
  }

  /* ===== MARQUEE ===== */
  .marquee-container {
    width: 100%;
    overflow: hidden;
    border-top: 2px solid #ccff00;
    border-bottom: 2px solid #ccff00;
    background: #0a0a0a;
    margin-top: 4rem;
  }

  .marquee {
    position: relative;
    width: 100%;
    height: 48px;
    overflow: hidden;
    white-space: nowrap;
  }

  .track {
    display: inline-flex;
    gap: 3rem;
    padding: 0.75rem 0;
    animation: marquee-move 20s linear infinite;
  }

  .track p {
    font-size: 0.85rem;
    font-weight: 700;
    color: #ccff00;
    text-transform: uppercase;
    text-shadow: 0 0 10px rgba(204, 255, 0, 0.5);
  }

  @keyframes marquee-move {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .marquee-container:hover .track {
    animation-play-state: paused;
  }
</style>
